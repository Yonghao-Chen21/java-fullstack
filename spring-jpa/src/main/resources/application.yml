spring:
   datasource:
     url: jdbc:mysql://localhost:3306/lwl
     username: dbuser
     password: dbuser
   jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
     properties:
       hibernate:
          dialect: org.hibernate.dialect.MySQL5Dialect
server:
 servlet:
   context-path: /ipl